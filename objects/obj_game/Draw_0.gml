// Desenhando a grid
for (var row = 0; row<ds_grid_height(grid); row++)
{
	for (var col = 0; col<ds_grid_width(grid); col++)
		{
			if (grid[# col, row] == -1)
			{
				continue;
			}
			//var subimage = grid[# col, row] + level;
			//instance_create_layer(col*160, row*160,"Instances",obj_piece)
			//draw_sprite(spr_char, subimage, col*160, row*160);
		}
	
}




